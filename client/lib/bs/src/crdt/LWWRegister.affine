// SPDX-License-Identifier: MPL-2.0
// Ported via Harvard Engine (Semantic pass)

module LWWRegister;

// SPDX-License-Identifier: MPL-2.0 OR Apache-2.0
// Copyright (c) 2024 Laniakea Contributors

/**
 * LWW-Register (Last-Writer-Wins Register) CRDT
 *
 * Simple single-value storage where highest timestamp wins.
 */

struct nodeId { string

struct t<'a> = {
  value: option<'a>,
  timestamp: int,
  nodeId: option<nodeId>,
  version: int,
}

/** Create a new, empty register */
fn make = (): t<'a> => {
  value: None,
  timestamp: 0,
  nodeId: None,
  version: 0,
}

/** Create with initial value */
fn makeWith = (value: 'a, nodeId: nodeId): t<'a> => {
  value: Some(value),
  timestamp: Js.Date.now()->Belt.Float.toInt,
  nodeId: Some(nodeId),
  version: 1,
}

/** Set the register value */
fn set = (reg: t<'a>, value: 'a, nodeId: nodeId): t<'a> => {
  {
    value: Some(value),
    timestamp: Js.Date.now()->Belt.Float.toInt,
    nodeId: Some(nodeId),
    version: reg.version + 1,
  }
}

/** Set with explicit timestamp (for testing) */
fn setWithTimestamp = (reg: t<'a>, value: 'a, nodeId: nodeId, timestamp: int): t<'a> => {
  {
    value: Some(value),
    timestamp,
    nodeId: Some(nodeId),
    version: reg.version + 1,
  }
}

/** Get the current value */
fn value = (reg: t<'a>): option<'a> => reg.value

/** Get timestamp */
fn timestamp = (reg: t<'a>): int => reg.timestamp

/** Get node ID */
fn nodeId = (reg: t<'a>): option<nodeId> => reg.nodeId

/** Merge two registers - highest timestamp wins */
fn merge = (a: t<'a>, b: t<'a>): t<'a> => {
  fn winner = if a.timestamp > b.timestamp {
    a
  } else if b.timestamp > a.timestamp {
    b
  } else {
    // Tie-breaker: lexicographic node ID comparison
    fn aNode = a.nodeId->Belt.Option.getWithDefault("")
    fn bNode = b.nodeId->Belt.Option.getWithDefault("")
    if aNode >= bNode {
      a
    } else {
      b
    }
  }

  {
    ...winner,
    version: Js.Math.max_int(a.version, b.version) + 1,
  }
}

// Wire format (generic value as JSON)
struct wireFormat<'a> = {
  @as("struct") struct_: string,
  value: Js.Nullable.t<'a>,
  timestamp: int,
  @as("node_id") nodeId: Js.Nullable.t<string>,
  version: int,
}

fn toWire = (reg: t<'a>): wireFormat<'a> => {
  struct_: "lww_register",
  value: reg.value->Js.Nullable.fromOption,
  timestamp: reg.timestamp,
  nodeId: reg.nodeId->Js.Nullable.fromOption,
  version: reg.version,
}

fn fromWire = (wire: wireFormat<'a>): t<'a> => {
  value: wire.value->Js.Nullable.toOption,
  timestamp: wire.timestamp,
  nodeId: wire.nodeId->Js.Nullable.toOption,
  version: wire.version,
}

